<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/colorMoccasin</item>
        <item name="colorControlActivated">@color/colorOrange</item>
    </style>
    <style name="RatingStyle" parent="android:style/Widget.Material.RatingBar.Small">
        <!-- 定义星星图片 -->

        <item name="android:progressDrawable">@drawable/ratingstars</item>
        <item name="android:minHeight">15dp</item>
        <item name="android:maxHeight">15dp</item>


    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    //需要換樣式的項目，style name="樣式名稱"
    //內容屬性項目的名稱(colorControlNormal, colorControlActivated ...)要上網去找，如果要定義顏色總表:
    res/values/colors.xml，或是直接輸入16位元字碼(#98fb98)也可以

    <style name="h2">
        <item name="android:textSize">@dimen/h2</item>
        <item name="android:textColor">@color/colorTextBlack</item>
    </style>

</resources>
